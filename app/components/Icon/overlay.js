import themeConfig from 'configurations/Theme.configuration';
import * as React from 'react';
import Svg, {Path, G, Defs} from 'react-native-svg';

function Overlay({color = 'yellow', ...props}) {
  return <></>;
}

export default Overlay;
